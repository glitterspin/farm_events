export default function DoesItWork() {
    return <>
        <h1>I think this should work</h1>
        <p> Testing to make sure I understand how this works, so this should be reachable @local:3000/posts/job-get. 
            Let's see if it works!
            Awesome it works. No formatting yet but whatever it's basic tutorial.</p>
    </>
}
